<h1>User info</h1>

<h1>New book</h1>
  <%= form_with model: @book do |f| %>
    <h4>Title</h4>
      <%= f.text_field :title %>
    <h4>Option</h4>
      <%= f.text_area :body %><br>
    <%= f.submit 'Create Book' %>
  <% end %>

<div>
  <h3><%= @user.name %></h3>
</div>

<table>
<thead>
  <tr>
   <th>　</th>
   <th>image</th>
   <th>name</th>
  </tr>
 </thead>
 <tbody>
 <% @user.each do |user| %>
  <tr>
   <td>
    <%= user.image %>
   </td>
   <td>
    <%= user.name %>
   </td>
  </tr>
 <% end %>
 </tbody>
</table>